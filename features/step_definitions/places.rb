When /^I add the new point of interest "([^"]*)" at longitude (.*) and latitude (.*)$/ do |name, longitude, latitude|
  pending
end

Then /^my account should contain the following points of interest:$/ do |table|
  pending
end

When /^the map should show the following features:$/ do |table|
  pending
end

When /^I try to add the new point of interest "([^"]*)" without placing it on the map$/ do |name|
  pending
end

When /^I try to add the new point of interest at longitude (.*) and latitude (.*) without naming it$/ do |longitude, latitude|
  pending
end